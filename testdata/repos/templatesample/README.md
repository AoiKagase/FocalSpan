# Smarty template sample

This fixture contains Smarty-like templates, bounded CSS, embedded JavaScript,
and a legacy mixed PHP template. It is data for FocalSpan acceptance tests;
FocalSpan never renders or executes these files.
