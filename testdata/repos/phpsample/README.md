# PHP sample repository

This fixture covers namespaced PHP classes, PHPUnit tests, a `.inc` bootstrap
include, and a mixed HTML/PHP template. It is intentionally indexed as source
data only; Composer and the PHP runtime are never executed by FocalSpan.
